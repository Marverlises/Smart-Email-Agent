# 隐私说明

Smart Email Reply 0.4.4 在浏览器原生侧边栏中运行。它只在用户主动点击生成按钮时，读取当前 Gmail 或 Outlook Web 页面已经加载的会话内容，并将其发送到用户自行配置的 LLM API。

## 不访问邮箱账户

- 不使用 Gmail API 或 Microsoft Graph
- 不申请 Gmail/Outlook OAuth
- 不需要 Microsoft Client ID 或 Tenant
- 不跨邮件线程搜索
- 不读取当前页面尚未加载的邮件
- 不会自动发送、修改或删除邮件

## 本地保存

- LLM API 地址、模型、API Key 和鉴权头
- 建议数量、语言、语气和上下文范围

这些数据保存在浏览器扩展本地存储中，并非操作系统级加密保险库。

## 发送到 LLM 的内容

- 当前会话中的发件人、收件人、时间、主题和正文
- 长会话的中间摘要
- 当前页面标题和生成偏好
- 用户在“按我的提示生成”输入框中填写的当前要求

长会话可能产生多个摘要请求。LLM 服务如何记录、保留或使用数据取决于相应服务商政策。

模型输出只以文本显示；扩展不执行模型返回的 HTML 或脚本。发送邮件前应检查事实、日期、金额、承诺、收件人和附件。
