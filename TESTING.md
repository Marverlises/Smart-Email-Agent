# 手工测试清单

## 安装与设置

- [ ] Chrome / Edge 能直接加载已解压目录。
- [ ] 点击工具栏图标打开浏览器原生侧边栏，而不是扩展弹窗或网页浮层。
- [ ] 在标签页 A 打开侧边栏后，切到未打开插件的标签页 B，A 的侧边栏不会显示在 B。
- [ ] A、B 分别打开插件后，两边的读取状态和生成结果互不覆盖。
- [ ] 标签页跳转或刷新后仍能点击图标打开其专属侧边栏，并自动重新读取。
- [ ] 每个侧边栏只向 URL 参数中的目标标签页 ID 读取和插入内容。
- [ ] 设置页不再出现 Gmail/Outlook OAuth、Client ID 或 Tenant。
- [ ] LLM API 可以保存并通过“测试连接”。
- [ ] 自定义提示为空时“根据提示生成”按钮不可用。
- [ ] 输入自定义提示后可点击按钮，并支持 `Ctrl/Command + Enter`。

## 当前会话读取

- [ ] Gmail 单封和多轮会话能识别正确消息数量。
- [ ] Outlook Web 单封和多轮会话能识别邮件数量；无法可靠拆分时明确显示“正文块”。
- [ ] `outlook.cloud.microsoft` 页面能连接侧边栏并读取当前邮件。
- [ ] Outlook 合并正文中的 `From/Sent/To/Subject` 引用链会被拆成多段历史邮件。
- [ ] 折叠消息会被尝试展开。
- [ ] “显示修剪内容”会被尝试展开。
- [ ] 隐藏但已加载的正文可以被提取。
- [ ] 相同正文不会被重复加入。

## 长会话和回复

- [ ] 短会话直接进入最终生成请求。
- [ ] 超出上下文限制时触发旧消息分批摘要。
- [ ] 最近消息在最终请求中保留详细内容。
- [ ] 返回 2–6 条不同建议。
- [ ] 自定义提示位于历史对话标记之外，并与完整历史一起进入最终生成请求。
- [ ] 自定义提示指定的语言、语气和任务优先于默认设置。
- [ ] 模型返回截断 JSON 时能恢复完整对象，并在数量不足时自动重试一次。
- [ ] 未解析的 `{"replies": ...}` 原始 JSON 不会显示为单条建议，也不能插入草稿。
- [ ] “复制”和“插入草稿”可用。
- [ ] 不会自动发送邮件。

## 错误与安全

- [ ] 非邮件会话页面显示明确提示。
- [ ] LLM 鉴权、网络和格式错误有明确提示。
- [ ] 模型返回 HTML 时只显示文本。
- [ ] 邮件中的提示注入文本不会改变摘要/回复任务。
